处理后得到的效果图如下：
（6）新建一个数据集dataset，它的行数是：一张图片中我们感兴趣的点（一个小像素），也就是：一个图片对象只考虑I_seg.sum()这么多像素，一共有61张图片，所以dataset将保存所有的图片数据信息，这样dataset每一列就是一张图片,而每一行就是每张图片同一个位置像素的信息。现在只是建好了一个行数是：I_seg.sum()，列数是：61的二维表，里面的元素现在都是0
（8）往dataset里面保存所有保存图片信息，每张R*I_seg图片同一个位置一个一个像素数据地保存，并且如果图片位置不是0的地方，就把它放进它对应在dataset的位置（dataset里面的元素原来都是0的），经过这个循环，61张图片我们感兴趣的像素点都保存在dataset里面了。